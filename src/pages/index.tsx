import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center py-2">
      <h1 className="text-2xl text-white">Working in Progress</h1>
    </div>
  );
};

export default Home;
